<?php
/*
	echo "Start Cleaning Block HTML cache ... \n\n";
	ini_set("display_errors", 1);
	 
	require 'app/Mage.php';
	Mage::app('admin')->setUseSessionInUrl(false);
	Mage::getConfig()->init();

	try {
		$type = 'block_html';
		flush();
		Mage::app()->getCacheInstance()->cleanType($type);
		echo "[OK]\n\n";
	} catch (exception $e) {
	die ("[ERROR:" . $e->getMessage() . "]");
	}
	
	*/
?>